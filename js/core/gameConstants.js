/**
 * Tutorial UI state constants
 */
export const TutorialUIState = {
    INACTIVE: 'inactive',
    ACTIVE: 'active',
    COMPLETED: 'completed'
};

/**
 * Input buffer constants (ms)
 * Prevent clicks within 250ms of last drop to avoid stacking
 */
export const INPUT_BUFFER_MS = 250;
